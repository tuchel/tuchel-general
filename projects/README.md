# Projects

One folder per project or idea that does not warrant its own repository.

Naming: prefer `{yyyy-mm}-{slug}/`. Use `{slug}/` when a date prefix is awkward.

Each project should start with a `README.md` (purpose, status, open questions). Add `raw/`, `notes/`, and code only as needed.

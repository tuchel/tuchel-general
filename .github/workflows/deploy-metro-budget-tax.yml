# Retained name for discoverability. Real deploy lives in deploy-pages.yml
# (tax + STAR MIND must ship in one Pages artifact).
name: Deploy Metro Budget & Tax Explorer

on:
  workflow_dispatch:

jobs:
  redirect:
    runs-on: ubuntu-latest
    steps:
      - name: Use Deploy GitHub Pages instead
        run: |
          echo "::notice::Run the workflow named 'Deploy GitHub Pages' (deploy-pages.yml)."
          echo "This alias no longer deploys on its own so the tax site and STAR MIND stay in sync."
          exit 0
